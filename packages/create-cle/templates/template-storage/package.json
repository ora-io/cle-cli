{
  "name": "cle-default-starter",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile-local": "cle compile --local",
    "setup-local": "cle setup --local",
    "exec-local": "cle exec --local",
    "prove-local": "cle prove --local",
    "upload-local": "cle upload --local",
    "compile": "cle compile",
    "setup": "cle setup",
    "exec": "cle exec",
    "prove": "cle prove",
    "upload": "cle upload",
    "verify": "cle verify",
    "publish": "cle publish",
    "deposit": "cle deposit"
  },
  "dependencies": {
    "@hyperoracle/cle-lib-test": "^1.0.1-alpha.1"
  },
  "devDependencies": {
    "@hyperoracle/cle-cli-test": "1.0.0-alpha.4",
    "assemblyscript": "^0.27.0"
  }
}
